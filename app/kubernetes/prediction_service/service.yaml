apiVersion: v1
kind: Service
metadata:
  name: prediction-service
  namespace: hatespeech
spec:
  selector:
    app: prediction-service
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
  type: ClusterIP
